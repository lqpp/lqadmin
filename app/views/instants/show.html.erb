<table border="1">
	<% @instants.attributes.each do |f| %>
		<% if f[1] %>
			<tr><td style="text-align: right"><%= f[0] %>:</td><td><%= f[1] %></td></tr>
		<% end %>
	<% end %>
</table>

<%= link_to 'Edit', edit_instant_path(@instants) %> |
<%= link_to 'Back', instants_path %>