
<p style="text-align: right"><%=link_to "Zurück", :action => "index" %></p>

<% form_tag("?", :method => "post") do %>
	Invete-Code:<%= text_field_tag(:such) %>
	<%= submit_tag("Suchen") %>
<% end %>

<% if @show then%>
	<table border="1">
	<% if @show[:status] then%>
		<tr>
			<td>Status:</td>
			<td><%=@show[:status]%></td>
		</tr>
	<% end %>
	<% if @show[:username] then%>
		<tr>
			<td>Username:</td>
			<td><%=@show[:username]%></td>
		</tr>
	<% end %>
	<% if @show[:userid] then%>
		<tr>
			<td>Profiel:</td>
			<td><%= link_to @show[:userid], "http://lqpp.de/"%></td>
		</tr>
	<% end %>
	<tr>
		<td>Aktion:</td>
		<td>
			<%= link_to "Send Username", ""%><br />
			<%= link_to "Send eMail", ""%>
		</td>
	</tr>
	</table>
<% end %>
