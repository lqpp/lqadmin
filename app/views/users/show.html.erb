<table border="1">
	<% @user.attributes.each do |f| %>
		<% if f[1] %>
			<tr><td style="text-align: right"><%= f[0] %>:</td><td><%= f[1] %></td></tr>
		<% end %>
	<% end %>
</table>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>